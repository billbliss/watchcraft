# Watchcraft web app

The portable React + TypeScript reader will live here. It consumes collection
packages through the shared repository interface and can be deployed as an
ordinary static web application.

The app must not import Electron, Python, filesystem APIs, or authored catalog
content into its source bundle.

