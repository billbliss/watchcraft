{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows only the native folder picker in the main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "dialog:allow-open"
  ]
}
